VITE_API_URL=http://localhost:3001

# Supabase Storage (fotos de saída, anexos de ocorrência, vídeos)
# Injetadas em BUILD TIME pelo Vite — em Docker vêm dos build args
# definidos em infra/.env (SUPABASE_URL / SUPABASE_ANON_KEY).
VITE_SUPABASE_URL=https://SEU_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=sua_anon_key_aqui
